
export const baseUrl = '/api/';

// In development mode, I had cors activated in my browser, so it was safe to use localhost.
// I then replaced it with proxy setup
// export const baseUrl = 'http://localhost:5000/api/';